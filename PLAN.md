# פסאו — דשבורד ניהולי | תוכנית MVP

> מסמך זה הוא "חוזה" המוצר: מה המטרה, מה כבר נבנה, ומה הצעדים הבאים.
> נועד גם להנחלת המערכת בעסק.

## 🎯 המטרה

פסאו כבר עובד — לכן זהו דשבורד **ניהולי**, לא דשבורד הקמה.
כל בוקר ב-09:00 פותחים מסך אחד, ותוך **5 דקות** יודעים:

- כמה כסף נכנס
- כמה אירועים נסגרו
- כמה לידים פתוחים
- איפה יש בעיות
- מה מצב השיווק
- מה מצב כוח האדם

## 🧭 החלטות מוצר (MVP)

| נושא | החלטה |
|------|--------|
| פלטפורמה | אפליקציית דשבורד עצמאית (React + TypeScript) |
| נתונים | נתוני דמה (Mock) מובנים, מוכן לחיבור אמיתי |
| סקופ | הכל — כולל מנוע אוטומציות |

## 🏗️ ארכיטקטורה

```
src/
├── types.ts              # מודל הדומיין (7 הקבוצות + התראות)
├── data/mockData.ts      # נתוני דמה יחסיים לתאריך "היום"
├── lib/
│   ├── dates.ts          # חישובי תאריכים (שבוע/חודש, ימים פתוח וכו')
│   ├── metrics.ts        # גזירת כל ה-KPI מהנתונים
│   ├── automations.ts    # מנוע 4 כללי האוטומציה -> התראות
│   └── format.ts         # פורמט ₪ ומספרים בעברית
├── components/           # רכיבי UI משותפים (כרטיסים, טבלאות, התראות)
└── pages/                # המסכים
```

עקרון מפתח: **כל המספרים נגזרים מהנתונים הגולמיים** (single source of truth).
מחליפים את `mockData.ts` במקור אמיתי — וכל הדשבורד מתעדכן אוטומטית.

## 📋 הקבוצות (לוח "פסאו - ניהול שוטף")

1. **מכירות** — תאריך, מחזור, סועדים, ממוצע לסועד, ממוצע שולחן, הערות
2. **אירועים** — לקוח, טלפון, סוג, אורחים, תאריך, סטטוס (ליד חדש → נסגר/אבוד), אחראי
3. **שיווק** — תוכן (פוסטים/רילס) וממומן (קמפיינים), תקציב, סטטוס
4. **ביקורות** — Google / OnTop / Facebook, דירוג, טופל, אחראי
5. **תחזוקה** — תקלה, אזור, נפתח, אחראי, עלות, סטטוס
6. **עובדים** — שם, תפקיד, תאריך התחלה, סטטוס
7. **ספקים** — ספק, תחום, איש קשר, טלפון, ימי אספקה

## 📺 המסכים

- **בקרת בעלים** (דף הבית) — 6 המספרים שפותחים כל בוקר
- **דשבורד מנכ״ל** — 10 הווידג׳טים המלאים + גרפים
- **KPI שבועי** — סיכום יום ראשון (מכירות/אירועים/שיווק/שירות)
- **אוטומציות והתראות** — הכללים + פיד ההתראות החי
- **7 מסכי קבוצות** — טבלאות נתונים מלאות עם סינון

## ⚡ אוטומציות חובה (מיושמות)

| כלל | טריגר | פעולה | נמען |
|-----|--------|--------|------|
| Follow Up | ליד חדש נכנס | משימה ל-24 שעות | מנהל אירועים |
| אירוע מתקרב | אירוע סגור בעוד ≤14 יום | התראת תיאום | מנהל אירועים |
| ביקורת שלילית | דירוג 1–3★ לא טופל | התראת מענה | מנהל מסעדה |
| תקלה תקועה | תקלה פתוחה >3 ימים | התראת טיפול | מנהל תפעול |

ההתראות בחומרה גבוהה מזינות את מונה **"משימות אדומות"** במסך הבעלים.

## 🚀 הרצה

```bash
npm install
npm run dev      # פיתוח
npm run build    # בנייה לפרודקשן
```

## 🔭 השלבים הבאים (אחרי ה-MVP)

1. **חיבור נתונים אמיתי** — אחת מהאינטגרציות שכבר מחוברות לסביבה:
   - **Supabase** — כבסיס נתונים/Backend אמיתי לקבוצות
   - **Make** — להרצת האוטומציות בפועל (שליחת התראות, וואטסאפ/מייל)
   - **OnTopo** — משיכת הזמנות/ביקורות
   - **Google Reviews API** — דירוג וביקורות אמת
   - **POS/קופה** — מחזור וסועדים אוטומטית
2. **השוואה לשנה קודמת** במכירות (דורש דאטה היסטורי)
3. **הזנה ידנית** — טפסים להוספת ליד/תקלה/מכירה ישירות מהדשבורד
4. **התראות בפוש** — וואטסאפ/מייל לנמענים בפועל
5. **הרשאות משתמשים** — בעלים מול מנהלים
```
